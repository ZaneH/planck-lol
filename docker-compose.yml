services:
  redis:
    image: redis:8
    container_name: planck-redis

  postgres:
    image: postgres:17
    container_name: planck-postgres
    ports:
      - 5432:5432
    environment:
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=postgres
      - POSTGRES_DB=postgres
